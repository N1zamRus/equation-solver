# Учебный курс по FFT для умножения больших чисел

Подробный материал по 5 блокам (≈7 часов изучения) с теоретической базой и разборами всех задач в конце.

---

## Содержание

- [Блок 1. Подготовка](#блок-1-подготовка)
  - [1.1. Многочлены и свёртка](#11-многочлены-и-свёртка)
  - [1.2. Комплексные числа](#12-комплексные-числа)
  - [1.3. Корни из единицы](#13-корни-из-единицы)
- [Блок 2. Идея DFT и переход к значениям в точках](#блок-2-идея-dft-и-переход-к-значениям-в-точках)
  - [2.1. Два представления многочлена](#21-два-представления-многочлена)
  - [2.2. DFT — это значения в корнях из единицы](#22-dft--это-значения-в-корнях-из-единицы)
- [Блок 3. Алгоритм FFT (Кули-Тьюки)](#блок-3-алгоритм-fft-кули-тьюки)
  - [3.1. Идея «разделяй и властвуй» для DFT](#31-идея-разделяй-и-властвуй-для-dft)
  - [3.2. Псевдокод рекурсивного FFT](#32-псевдокод-рекурсивного-fft)
- [Блок 4. Применение к умножению больших чисел](#блок-4-применение-к-умножению-больших-чисел)
  - [4.1. От свёртки коэффициентов к большому числу](#41-от-свёртки-коэффициентов-к-большому-числу)
  - [4.2. Точность float и борьба с ней](#42-точность-float-и-борьба-с-ней)
  - [4.3. Сборка финального кода умножения](#43-сборка-финального-кода-умножения)
- [Блок 5. Резерв и систематизация](#блок-5-резерв-и-систематизация)
- [Разборы всех задач](#разборы-всех-задач)

---

# Блок 1. Подготовка

## 1.1. Многочлены и свёртка

### Что такое многочлен

Многочлен (полином) от переменной $x$ — это выражение вида:

$$A(x) = a_0 + a_1 x + a_2 x^2 + \dots + a_{n-1} x^{n-1}$$

Числа $a_0, a_1, \dots, a_{n-1}$ — **коэффициенты**. Наибольшая степень $x$, при которой коэффициент не ноль, — **степень многочлена**.

Примеры:
- $A(x) = 3 + 5x + 2x^2$ — степень 2, коэффициенты $(3, 5, 2)$.
- $B(x) = 7$ — степень 0, коэффициенты $(7)$.
- $C(x) = x^3$ — степень 3, коэффициенты $(0, 0, 0, 1)$.

Многочлен **полностью задаётся списком коэффициентов**. Никакой другой информации не нужно.

### Связь с большими числами

Большое число в позиционной системе с основанием $B$ — это в точности значение многочлена в точке $B$.

Пример: число 12345678 в системе с основанием 10000 представляется chunks $(5678, 1234)$. Это многочлен:

$$A(x) = 5678 + 1234 x$$

И исходное число — это $A(10000) = 5678 + 1234 \cdot 10000 = 12{,}345{,}678$.

Поэтому **умножать длинные числа = умножать соответствующие многочлены**. Если $X = A(B)$ и $Y = A'(B)$, то $X \cdot Y = (A \cdot A')(B)$. Достаточно научиться перемножать многочлены, а потом подставить $B$ — это уже тривиальный проход с переносами.

### Умножение многочленов

Перемножить два многочлена — это раскрыть скобки и привести подобные:

$$(1 + 2x)(3 + x) = 1 \cdot 3 + 1 \cdot x + 2x \cdot 3 + 2x \cdot x = 3 + x + 6x + 2x^2 = 3 + 7x + 2x^2$$

Если у $A$ степень $n-1$, а у $B$ степень $m-1$, то у $A \cdot B$ степень $(n-1) + (m-1) = n + m - 2$. Количество коэффициентов: $n + m - 1$.

### Свёртка как формула для коэффициентов

Если расписать произведение в общем виде:

$$A(x) = \sum_{i=0}^{n-1} a_i x^i, \quad B(x) = \sum_{j=0}^{m-1} b_j x^j$$

то

$$A(x) \cdot B(x) = \sum_{i,j} a_i b_j \cdot x^{i+j}$$

Чтобы получить коэффициент при $x^k$, нужно собрать все слагаемые с $i + j = k$:

$$c_k = \sum_{i + j = k} a_i \cdot b_j = \sum_{i=0}^{k} a_i \cdot b_{k-i}$$

Эта операция называется **свёрткой** последовательностей $(a_i)$ и $(b_j)$. Запись: $c = a * b$.

### Стоимость наивной свёртки

Чтобы посчитать все $c_k$, нужно для каждого из $n + m - 1$ значений $k$ перебрать все пары $(i, j)$ с $i + j = k$. Итого — порядка $n \cdot m$ умножений и сложений. Если $n \approx m$, это $O(n^2)$.

Для двух чисел по 10000 цифр (примерно 2500 chunks с `BASE=10000`) это $2500^2 \approx 6 \cdot 10^6$ операций. Терпимо, но FFT даёт $O(n \log n) \approx 30{,}000$ — на два порядка быстрее.

### Проверочные задачи к 1.1

1. Перемножьте многочлены $A(x) = 1 + x + x^2$ и $B(x) = 2 + 3x$ — выпишите коэффициенты результата.
2. Запишите число 50000000 в системе с `BASE = 1000` как chunks (little-endian) и как многочлен. Проверьте подстановкой $x = 1000$.
3. Сколько коэффициентов будет у произведения двух многочленов, если у первого 4 коэффициента, а у второго 6?

---

## 1.2. Комплексные числа

### Зачем они нужны

Уравнение $x^2 = -1$ не имеет решений среди действительных чисел. Математики ввели новый объект $i$ по определению такой, что $i^2 = -1$, и расширили арифметику.

**Комплексное число** — это выражение $z = a + bi$, где $a, b$ — действительные. $a$ — действительная часть ($\text{Re}\, z$), $b$ — мнимая ($\text{Im}\, z$).

Арифметика — складываются и умножаются как обычные двучлены, помня, что $i^2 = -1$.

**Сложение:**

$$(a + bi) + (c + di) = (a + c) + (b + d) i$$

**Умножение:**

$$(a + bi)(c + di) = ac + adi + bci + bdi^2$$

Заменяем $i^2 = -1$:

$$(a + bi)(c + di) = (ac - bd) + (ad + bc) i$$

**Численный пример:** $(2 + 3i)(1 + 4i)$. По формуле: $a=2, b=3, c=1, d=4$.
- Действительная часть: $ac - bd = 2 - 12 = -10$
- Мнимая часть: $ad + bc = 8 + 3 = 11$
- Результат: $-10 + 11i$

### Геометрическое представление

Комплексное число $a + bi$ — это **точка на плоскости** с координатами $(a, b)$. Горизонтальная ось — действительная, вертикальная — мнимая. Это даёт мощную интуицию: комплексные числа — это **двумерные числа**.

### Модуль и аргумент

Точку на плоскости можно описать двумя способами:
- **Декартовы координаты** $(a, b)$.
- **Полярные координаты** $(r, \varphi)$, где $r$ — расстояние от нуля, $\varphi$ — угол с положительной осью Re.

Связь:

$$a = r \cos \varphi, \quad b = r \sin \varphi$$

$$r = \sqrt{a^2 + b^2}, \quad \varphi = \arctan(b/a)$$

$r$ — **модуль** комплексного числа, $|z|$. $\varphi$ — **аргумент**, $\arg z$.

Например, для $z = 1 + i$: $r = \sqrt{2}$, $\varphi = 45°$ или $\pi/4$.

### Формула Эйлера

Утверждение, которое нужно принять (вывод — в матанализе):

$$e^{i\varphi} = \cos \varphi + i \sin \varphi$$

С её помощью любое комплексное число записывается в **показательной форме**:

$$z = r \cdot e^{i\varphi}$$

Смысл: «число с модулем $r$ и углом $\varphi$».

### Почему это удобно — умножение

В показательной форме умножение тривиально:

$$z_1 \cdot z_2 = (r_1 e^{i\varphi_1}) \cdot (r_2 e^{i\varphi_2}) = r_1 r_2 \cdot e^{i(\varphi_1 + \varphi_2)}$$

То есть **модули умножаются, аргументы складываются**. Геометрически: умножение на $r e^{i\varphi}$ — это **поворот на угол $\varphi$ + растяжение в $r$ раз**. Если $r = 1$, это просто поворот.

Это будет ключевой механизм во всём FFT.

### Проверочные задачи к 1.2

1. Запишите $z = 2 + 2i$ в показательной форме.
2. Чему равно $i \cdot i$ (а) в декартовой записи через $i^2 = -1$, (б) в показательной записи через формулу Эйлера? (Подсказка: $i = e^{i\pi/2}$.)
3. На какой угол поворачивает плоскость умножение на $-1$? А на $i$?

---

## 1.3. Корни из единицы

### Уравнение $z^n = 1$

В комплексных числах решений всегда **ровно $n$**. Запишем $z = r e^{i\varphi}$:

$$z^n = r^n e^{in\varphi} = 1$$

Единица: $1 = 1 \cdot e^{i \cdot 0}$. Сравниваем модули и аргументы:
- $r^n = 1$, значит $r = 1$.
- $n \varphi = 2\pi k$ для целых $k$, значит $\varphi = \frac{2\pi k}{n}$.

При $k = 0, 1, \dots, n-1$ получаем $n$ различных углов.

### Стандартные обозначения

$k$-й корень $n$-й степени:

$$\omega_n^k = e^{2\pi i k / n}$$

$\omega_n = \omega_n^1 = e^{2\pi i / n}$ — **главный корень степени $n$**. Все остальные — его степени:

$$\omega_n^0 = 1, \quad \omega_n^1 = \omega_n, \quad \dots, \quad \omega_n^{n-1} = (\omega_n)^{n-1}$$

### Геометрическое расположение

Все $n$ корней лежат на единичной окружности и расставлены равномерно — между соседними угол $2\pi / n$.

Например, для $n = 4$:
- $\omega_4^0 = 1$ (угол 0°)
- $\omega_4^1 = i$ (угол 90°)
- $\omega_4^2 = -1$ (угол 180°)
- $\omega_4^3 = -i$ (угол 270°)

### Три ключевых свойства

Все три используются в FFT.

**Свойство 1 (периодичность):** $\omega_n^{k+n} = \omega_n^k$. Дойдя до $n$-го корня, возвращаемся к началу.

**Свойство 2 (симметрия):** $\omega_n^{k + n/2} = -\omega_n^k$. Корни в «противоположных» точках отличаются знаком.

**Свойство 3 (сжатие):** $\omega_n^{2k} = \omega_{n/2}^k$. Каждый второй корень из набора $n$ совпадает с корнем из набора $n/2$.

Доказательство свойства 3 на одну строку через формулу Эйлера:

$$\omega_n^{2k} = e^{2\pi i \cdot 2k / n} = e^{2\pi i k / (n/2)} = \omega_{n/2}^k$$

### Проверочные задачи к 1.3

1. Выпишите все корни 6-й степени из единицы: $\omega_6^0, \dots, \omega_6^5$. Укажите углы и точные значения через $\sqrt{3}$.
2. Проверьте свойство симметрии для $n = 8, k = 1$: вычислите $\omega_8^1$ и $\omega_8^5$ через формулу Эйлера и убедитесь, что они отличаются знаком.
3. Сколько разных значений принимает $\omega_8^{2k}$ при $k = 0, 1, \dots, 7$? Какие именно?

---

# Блок 2. Идея DFT и переход к значениям в точках

## 2.1. Два представления многочлена

### Зачем два представления

У многочлена есть две эквивалентные записи:

1. **Через коэффициенты** — список чисел при степенях $x$.
2. **Через значения в точках** — список чисел, которые многочлен принимает в выбранных точках.

Эти два списка содержат **одну и ту же информацию о многочлене** (если точек выбрано достаточно), но операции над ними имеют **разную стоимость**.

| Операция | Через коэффициенты | Через значения |
|---|---|---|
| Сложение многочленов | $O(n)$ | $O(n)$ |
| Умножение на число | $O(n)$ | $O(n)$ |
| **Умножение многочленов** | **$O(n^2)$ — свёртка** | **$O(n)$ — поточечно** |
| Вычисление в одной точке | $O(n)$ — Горнер | $O(n^2)$ — нужна интерполяция |

Видно: умножение многочленов в коэффициентах дорогое, в значениях — дешёвое.

### Эквивалентность представлений

**Утверждение:** многочлен степени $< n$ полностью задаётся своими значениями в любых $n$ различных точках.

**Почему — система уравнений.** Если у нас многочлен $A(x) = a_0 + a_1 x + a_2 x^2$ и значения $A(0)=3, A(1)=10, A(2)=21$, то:

$$A(0) = a_0 = 3$$
$$A(1) = a_0 + a_1 + a_2 = 10$$
$$A(2) = a_0 + 2 a_1 + 4 a_2 = 21$$

Три уравнения, три неизвестных. Решая, получаем $a_0 = 3, a_1 = 5, a_2 = 2$.

В общем виде: $n$ значений в $n$ точках дают систему $n$ уравнений с $n$ неизвестными $a_0, \dots, a_{n-1}$. Решение единственно — если все точки $x_i$ различны.

**Геометрическая интуиция.** Через 2 различные точки проходит ровно 1 прямая. Через 3 — ровно 1 парабола. Через $n$ — ровно 1 многочлен степени $< n$.

### Поточечное умножение

**Утверждение:** если $C(x) = A(x) \cdot B(x)$, то для любой точки $x_k$:

$$C(x_k) = A(x_k) \cdot B(x_k)$$

Это просто определение умножения многочленов как функций. Если есть значения $A$ и $B$ в одной и той же точке — значение $C$ это их произведение.

### Сколько точек нужно

Если у $A$ — $n$ коэффициентов, у $B$ — $m$, то у $C = A \cdot B$ — $n + m - 1$ коэффициентов. Чтобы представить $C$ через значения, нужно **минимум $n + m - 1$ различных точек**.

### Полный алгоритм умножения через «значения»

1. **Выбрать $N$ точек** $x_0, \dots, x_{N-1}$, где $N \geq n + m - 1$.
2. **Перевод $A$ в значения:** вычислить $A(x_0), \dots, A(x_{N-1})$.
3. **Перевод $B$ в значения** в тех же точках.
4. **Поточечное умножение:** $C(x_k) = A(x_k) \cdot B(x_k)$ — $O(N)$.
5. **Обратный перевод:** из значений восстановить коэффициенты $C$.

Шаг 4 — линейный, дешёвый. Узкие места — шаги 2, 3 и 5.

**При произвольных точках:** шаги 2, 3 стоят $O(N \cdot n) = O(N^2)$, шаг 5 — $O(N^3)$ через метод Гаусса. Полная стоимость хуже наивной свёртки. **Никакого выигрыша.**

**При корнях из единицы:** все шаги стоят $O(N \log N)$ через FFT. Полная стоимость — $O(N \log N)$.

То есть **сама идея «перейти в значения»** не даёт выигрыша. Выигрыш даёт **специальный выбор точек**.

### Проверочные задачи к 2.1

1. У вас многочлен $A(x) = 1 + 2x + x^2$. Запишите его в представлении через значения в точках $x_0 = -1, x_1 = 0, x_2 = 1$.
2. Вам сказали: «многочлен $B$ степени не выше 1 даёт в точках $0$ и $2$ значения $5$ и $11$». Найдите коэффициенты $B$.
3. У многочленов $A$ и $B$ — по 100 коэффициентов. Сколько точек нужно как минимум, чтобы представить их произведение через значения? А чтобы представить сами $A$ и $B$?

---

## 2.2. DFT — это значения в корнях из единицы

### Определение

**Дискретное преобразование Фурье (DFT)** — это «перевод в значения», где точки — корни $n$-й степени из единицы.

Дано: многочлен $A(x) = \sum_{j=0}^{n-1} a_j x^j$ с $n$ коэффициентами.

**Прямое DFT** возвращает значения в точках $\omega_n^0, \omega_n^1, \dots, \omega_n^{n-1}$:

$$\hat{a}_k = A(\omega_n^k) = \sum_{j=0}^{n-1} a_j \cdot \omega_n^{jk}, \quad k = 0, \dots, n-1$$

Шапочка над буквой — стандартное обозначение для результата DFT.

### Матричная запись

$\hat{a} = W \cdot a$, где $W_{kj} = \omega_n^{jk}$. Полезно для анализа, но в коде не нужно — вычисление в лоб через матрицу стоит $O(n^2)$. Цель FFT — за $O(n \log n)$ без явного построения $W$.

### Обратное DFT

$$a_j = \frac{1}{n} \sum_{k=0}^{n-1} \hat{a}_k \cdot \omega_n^{-jk}$$

Сравните:
- Прямое: $\hat{a}_k = \sum_j a_j \cdot \omega_n^{jk}$
- Обратное: $a_j = \frac{1}{n} \sum_k \hat{a}_k \cdot \omega_n^{-jk}$

Отличия минимальны: знак показателя у $\omega$ и деление на $n$ в конце. **Тот же код FFT работает для обоих** — нужно подставить $\omega_n^{-1}$ вместо $\omega_n$ и поделить результат на $n$.

### Свёртка через DFT

Для умножения многочленов $A$ и $B$:

1. Дополнить коэффициенты обоих нулями до длины $N$.
2. Прямое DFT обоих: $\hat{A}, \hat{B}$.
3. Поточечное умножение: $\hat{C}_k = \hat{A}_k \cdot \hat{B}_k$.
4. Обратное DFT: получаем коэффициенты $C$.

**Какое $N$ выбрать.** $N \geq n + m - 1$. На практике — ближайшая сверху степень двойки.

**Точность.** FFT работает с комплексными float-ами; результат свёртки целых коэффициентов получается как `2.9999998 + 0.0000001i` вместо `3`. В коде — округление к ближайшему целому.

### Связь с свойствами корней

Все три свойства из 1.3 работают на нас:

- **Периодичность** $\omega_n^{k+n} = \omega_n^k$ позволяет считать индексы по модулю $n$.
- **Симметрия** $\omega_n^{k + n/2} = -\omega_n^k$ даёт вторую половину FFT даром.
- **Сжатие** $\omega_n^{2k} = \omega_{n/2}^k$ позволяет рекурсивно вызывать FFT для половинного размера.

Без этих свойств был бы $O(n^2)$. С ними — $O(n \log n)$.

### Проверочные задачи к 2.2

1. Запишите формулу прямого DFT для $n = 4$ как 4 уравнения (распишите сумму явно для каждого $k = 0, 1, 2, 3$).
2. Возьмите $A(x) = 1 + x$ ($n = 2$, $a_0 = 1, a_1 = 1$). Вычислите $\hat{a}_0$ и $\hat{a}_1$ — DFT длины 2. Подсказка: $\omega_2 = e^{i\pi} = -1$.
3. Объясните в одном-двух предложениях: почему алгоритм «в лоб» (вычислить $A(\omega_n^k)$ для каждого $k$ независимо через схему Горнера) стоит $O(n^2)$, а не $O(n \log n)$? Что в нём не использует свойства корней?

---

# Блок 3. Алгоритм FFT (Кули-Тьюки)

## 3.1. Идея «разделяй и властвуй» для DFT

### Постановка

Дано: многочлен $A(x)$ с $n$ коэффициентами, $n$ — степень двойки. Нужно: вычислить значения $\hat{a}_k = A(\omega_n^k)$ для $k = 0, \dots, n-1$.

В лоб: $O(n^2)$. Хотим $O(n \log n)$ через стратегию «разделяй и властвуй»: сводим задачу размера $n$ к двум задачам размера $n/2$ за линейное время. Тогда $T(n) = 2T(n/2) + O(n) = O(n \log n)$.

### Шаг 1. Разделение многочлена

Группируем коэффициенты по чётности индекса:

$$A(x) = \underbrace{(a_0 + a_2 x^2 + a_4 x^4 + \dots)}_{\text{чётные}} + \underbrace{(a_1 x + a_3 x^3 + \dots)}_{\text{нечётные}}$$

Из нечётной части выносим $x$:

$$A(x) = (a_0 + a_2 x^2 + a_4 x^4 + \dots) + x \cdot (a_1 + a_3 x^2 + a_5 x^4 + \dots)$$

В обеих скобках только чётные степени $x$. Заменяем $x^2$ на новую переменную $y$:

$$A_{even}(y) = a_0 + a_2 y + a_4 y^2 + \dots + a_{n-2} y^{n/2 - 1}$$
$$A_{odd}(y) = a_1 + a_3 y + a_5 y^2 + \dots + a_{n-1} y^{n/2 - 1}$$

Это **два новых многочлена половинного размера** — у каждого $n/2$ коэффициентов. Тогда:

$$A(x) = A_{even}(x^2) + x \cdot A_{odd}(x^2)$$

**Пример для $n = 4$.** $A(x) = a_0 + a_1 x + a_2 x^2 + a_3 x^3$.
- $A_{even}(y) = a_0 + a_2 y$
- $A_{odd}(y) = a_1 + a_3 y$
- Проверка: $A_{even}(x^2) + x \cdot A_{odd}(x^2) = (a_0 + a_2 x^2) + x(a_1 + a_3 x^2) = A(x)$ ✓

### Шаг 2. Подставляем $\omega_n^k$

$$A(\omega_n^k) = A_{even}((\omega_n^k)^2) + \omega_n^k \cdot A_{odd}((\omega_n^k)^2)$$

Используем свойство сжатия: $(\omega_n^k)^2 = \omega_n^{2k} = \omega_{n/2}^k$. Получаем:

$$A(\omega_n^k) = A_{even}(\omega_{n/2}^k) + \omega_n^k \cdot A_{odd}(\omega_{n/2}^k)$$

**Это ключевая формула FFT.** В правой части — DFT длины $n/2$ для $A_{even}$ и $A_{odd}$.

Если у нас уже посчитаны:
- $\hat{e}_k = A_{even}(\omega_{n/2}^k)$
- $\hat{o}_k = A_{odd}(\omega_{n/2}^k)$

то:

$$\hat{a}_k = \hat{e}_k + \omega_n^k \cdot \hat{o}_k$$

### Шаг 3. Симметрия для второй половины

У $\hat{e}$ и $\hat{o}$ всего $n/2$ значений ($k = 0, \dots, n/2 - 1$). А у $\hat{a}$ должно быть $n$ значений. Что с верхней половиной — $k$ от $n/2$ до $n - 1$?

Используем симметрию $\omega_n^{k + n/2} = -\omega_n^k$:

$$\hat{a}_{k + n/2} = A_{even}(\omega_{n/2}^{k + n/2}) + \omega_n^{k + n/2} \cdot A_{odd}(\omega_{n/2}^{k + n/2})$$

- $\omega_{n/2}^{k + n/2} = \omega_{n/2}^k$ (периодичность для размера $n/2$)
- $\omega_n^{k + n/2} = -\omega_n^k$

$$\hat{a}_{k + n/2} = \hat{e}_k - \omega_n^k \cdot \hat{o}_k$$

**Получили вторую половину даром.** Сравните:

$$\hat{a}_k = \hat{e}_k + \omega_n^k \cdot \hat{o}_k \quad (k = 0, \dots, n/2 - 1)$$
$$\hat{a}_{k + n/2} = \hat{e}_k - \omega_n^k \cdot \hat{o}_k \quad (k = 0, \dots, n/2 - 1)$$

Одно и то же выражение, только знак различается. Произведение $\omega_n^k \cdot \hat{o}_k$ вычисляется **один раз** и переиспользуется. Это называется **бабочкой (butterfly)**.

### Шаг 4. Стоимость

На уровне рекурсии (размер $n$):
1. Разделить коэффициенты — $O(n)$.
2. Два рекурсивных вызова размера $n/2$.
3. Скомбинировать через бабочки — $O(n)$.

Рекуррента: $T(n) = 2T(n/2) + O(n) = O(n \log n)$.

**Базовый случай.** При $n = 1$ многочлен — это $A(x) = a_0$, его DFT длины 1 — это сам $a_0$. Возвращаем массив как есть.

### Зачем степень двойки

Чтобы рекурсия дошла до базового случая, $n$ должно делиться пополам много раз подряд. При $n = 6$: $6 \to 3 \to ?$ — застревание. При $n = 8$: $8 \to 4 \to 2 \to 1$ — корректно.

Поэтому массивы дополняются нулями до ближайшей сверху степени двойки.

### Проверочные задачи к 3.1

1. Возьмём $n = 8$. Сколько раз можно поделить массив пополам, прежде чем дойдём до базового случая? Сколько уровней рекурсии получится?
2. Распишите шаг рекурсии для $n = 4$: для $A(x) = a_0 + a_1 x + a_2 x^2 + a_3 x^3$ выпишите $A_{even}, A_{odd}$ и формулы для $\hat{a}_0, \hat{a}_1, \hat{a}_2, \hat{a}_3$ через $\hat{e}_0, \hat{e}_1, \hat{o}_0, \hat{o}_1$ и корни $\omega_4^k$.
3. Что бы сломалось без свойства симметрии $\omega_n^{k+n/2} = -\omega_n^k$?

---

## 3.2. Псевдокод рекурсивного FFT

### Структура функции

Функция принимает массив коэффициентов и **флаг направления** (прямое или обратное). Возвращает массив значений.

```
function fft(a, invert):
    n = length(a)
    if n == 1:
        return a    # базовый случай

    # Шаг 1: разделение
    a_even = [a[0], a[2], a[4], ..., a[n-2]]
    a_odd  = [a[1], a[3], a[5], ..., a[n-1]]

    # Шаг 2: рекурсия
    e = fft(a_even, invert)
    o = fft(a_odd, invert)

    # Шаг 3: бабочки
    result = array of length n
    for k from 0 to n/2 - 1:
        if invert:
            w = exp(-2 * π * i * k / n)
        else:
            w = exp(2 * π * i * k / n)

        t = w * o[k]
        result[k]       = e[k] + t
        result[k + n/2] = e[k] - t

    return result
```

### Деление на $n$ при обратном FFT

Если делить на $n$ внутри функции на каждом уровне рекурсии — поделим в общей сложности на $n^{\log n}$. **Неправильно.**

Правильно: считать обычный FFT с инвертированным знаком, потом **один раз** на верхнем уровне поделить на $n$:

```
function ifft(a):
    result = fft(a, invert=True)
    for i in range(len(result)):
        result[i] /= len(result)
    return result
```

### Реализация на Python

```python
import cmath

def fft(a, invert=False):
    n = len(a)
    if n == 1:
        return list(a)

    a_even = a[0::2]
    a_odd  = a[1::2]

    e = fft(a_even, invert)
    o = fft(a_odd,  invert)

    angle_sign = -1 if invert else 1

    result = [0] * n
    for k in range(n // 2):
        angle = angle_sign * 2 * cmath.pi * k / n
        w = cmath.exp(1j * angle)
        t = w * o[k]
        result[k]          = e[k] + t
        result[k + n // 2] = e[k] - t

    return result


def ifft(a):
    n = len(a)
    result = fft(a, invert=True)
    return [x / n for x in result]
```

Замечания:
- `a[0::2]` — каждый второй элемент с нуля → чётные индексы.
- `cmath.exp(1j * angle)` — комплексная экспонента; `1j` это $i$.
- Внутри рекурсии массивы — комплексные числа.

### Применение к умножению многочленов

```python
def multiply_polynomials(a, b):
    result_size = len(a) + len(b) - 1
    n = 1
    while n < result_size:
        n *= 2

    a_padded = list(a) + [0] * (n - len(a))
    b_padded = list(b) + [0] * (n - len(b))

    fa = fft(a_padded)
    fb = fft(b_padded)

    fc = [fa[i] * fb[i] for i in range(n)]

    c_complex = ifft(fc)

    c = [round(x.real) for x in c_complex[:result_size]]
    return c
```

### Контроль работы на маленьком примере

Умножим $A(x) = 1 + 2x$ на $B(x) = 3 + x$. Ручной ответ: $C(x) = 3 + 7x + 2x^2$.

**Размер.** `result_size = 3`. Ближайшая степень двойки — $n = 4$.

**Паддинг.** `a_padded = [1, 2, 0, 0]`, `b_padded = [3, 1, 0, 0]`.

**Прямое FFT $a$.** Корни: $\omega_4^0 = 1, \omega_4^1 = i, \omega_4^2 = -1, \omega_4^3 = -i$.

$\hat{a}_k = \sum_j a_j \omega_4^{jk}$:
- $\hat{a}_0 = 1 + 2 + 0 + 0 = 3$
- $\hat{a}_1 = 1 + 2i$
- $\hat{a}_2 = 1 - 2 = -1$
- $\hat{a}_3 = 1 - 2i$

**FFT $b$:** $\hat{b} = (4, 3+i, 2, 3-i)$.

**Поточечно:**
- $\hat{c}_0 = 12$
- $\hat{c}_1 = (1 + 2i)(3 + i) = 1 + 7i$
- $\hat{c}_2 = -2$
- $\hat{c}_3 = 1 - 7i$

**Обратное FFT** даёт $c = (3, 7, 2, 0)$. Обрезаем до 3: $(3, 7, 2)$. ✓

### Проверочные задачи к 3.2

1. Что произойдёт, если в `fft` забыть базовый случай (`if n == 1: return a`)?
2. В `multiply_polynomials` написано `c_complex[:result_size]` — обрезание. Зачем оно нужно?
3. Прокрутите алгоритм для $A(x) = 1$, $B(x) = 1 + x$. Должно получиться $C(x) = 1 + x$. Какой будет $n$? Какие массивы после паддинга?

---

# Блок 4. Применение к умножению больших чисел

## 4.1. От свёртки коэффициентов к большому числу

### Постановка

Структура `BigFloat`:
- `sign` — $+1$ или $-1$.
- `chunks` — кортеж little-endian, значения в $[0, \text{BASE})$.
- `exponent` — отрицательное целое.

Произведение двух BigFloat:

$$X \cdot Y = (s_X \cdot s_Y) \cdot \left(\sum_i x_i \text{BASE}^i\right) \cdot \left(\sum_j y_j \text{BASE}^j\right) \cdot 10^{e_X + e_Y}$$

Три отдельные части:
- **Знак:** $s_X \cdot s_Y$.
- **Экспонента:** $e_X + e_Y$.
- **Chunks:** свёртка `chunks` $X$ и $Y$ + нормализация переносов.

```python
def multiplication(bigf1, bigf2):
    new_sign = bigf1.sign * bigf2.sign
    new_exponent = bigf1.exponent + bigf2.exponent
    new_chunks = multiply_chunks(bigf1.chunks, bigf2.chunks)
    return normalize(BigFloat(new_sign, new_chunks, new_exponent))
```

### Шаг 1. Свёртка через FFT

```python
def multiply_chunks_via_fft(x_chunks, y_chunks):
    a = list(x_chunks)
    b = list(y_chunks)

    result_size = len(a) + len(b) - 1
    n = 1
    while n < result_size:
        n *= 2

    a_padded = a + [0] * (n - len(a))
    b_padded = b + [0] * (n - len(b))

    fa = fft(a_padded)
    fb = fft(b_padded)

    fc = [fa[i] * fb[i] for i in range(n)]

    c_complex = ifft(fc)

    raw_chunks = [round(x.real) for x in c_complex[:result_size]]
    return raw_chunks
```

На выходе — список $c_k$. Каждое — сумма произведений $x_i \cdot y_j$ для $i + j = k$. Эти числа могут быть больше `BASE` — нужна нормализация.

### Шаг 2. Carry — приведение в `[0, BASE)`

Пример при `BASE = 10`: после свёртки `[12, 27, 5]` представляет $12 + 270 + 500 = 782$. Нормализуем:
- `12` = `2 + 1 * BASE` → кладём 2, переносим 1.
- `27 + 1 = 28` = `8 + 2 * BASE` → кладём 8, переносим 2.
- `5 + 2 = 7` → кладём 7.

Результат: `[2, 8, 7]`. Это $2 + 80 + 700 = 782$. ✓

```python
def apply_carry(raw_chunks, base):
    result = []
    carry = 0
    for c in raw_chunks:
        total = c + carry
        result.append(total % base)
        carry = total // base

    while carry > 0:
        result.append(carry % base)
        carry //= base

    return tuple(result)
```

### Проверочные задачи к 4.1

1. $X = 12, Y = 13$ при `BASE = 10`. Распишите chunks, посчитайте свёртку вручную, примените `apply_carry`. Должно получиться $156$.
2. `raw = [0, 0, 200]` при `BASE = 100`. Что вернёт `apply_carry`? Какому числу это соответствует?
3. Почему важно складывать экспоненты? Распишите: $X = 12.5$ (chunks=(125,), exp=-1), $Y = 0.04$ (chunks=(4,), exp=-2). Какая экспонента у $X \cdot Y$?

---

## 4.2. Точность float и борьба с ней

### Проблема

FFT работает с `complex` (double, ~15-16 значащих цифр). После операций целые $c_k$ получаются как `4999.99999998`. Округление это исправляет — **если ошибка достаточно мала**.

### Оценка $c_k$

$c_k = \sum_{i+j=k} a_i b_j$. Каждое слагаемое — до $\text{BASE}^2$. Слагаемых до $n$. Итого: $c_k \leq n \cdot \text{BASE}^2$.

Для 10000 символов и `BASE = 10000`: $n \approx 2500$ chunks, после паддинга $N = 4096$. Худшее $c_k$:

$$c_k^{max} \approx 4096 \cdot 10^8 \approx 4 \cdot 10^{11}$$

Около 12 десятичных цифр. У float остаётся 3 цифры запаса. **На грани.**

### Решение: дробление chunks

При `BASE = 100` (2 цифры): $n \approx 5000$, после паддинга $\approx 8192$. $c_k^{max} \approx 8192 \cdot 10^4 \approx 10^8$ — 8 цифр. Запас 7 цифр, гарантированно сходится.

Архитектура: внешний `BASE = 10000`, внутри FFT — `SMALL_BASE = 100`. Дробление и склейка — линейные операции, не портят асимптотику.

```python
def split_chunks(chunks, small_base, ratio):
    result = []
    for c in chunks:
        for _ in range(ratio):
            result.append(c % small_base)
            c //= small_base
    return result


def join_chunks(small_chunks, small_base, ratio):
    result = []
    i = 0
    while i < len(small_chunks):
        c = 0
        multiplier = 1
        for j in range(ratio):
            if i + j < len(small_chunks):
                c += small_chunks[i + j] * multiplier
            multiplier *= small_base
        result.append(c)
        i += ratio
    return result
```

### Полная функция умножения с защитой от потери точности

```python
SMALL_BASE = 100
RATIO = 2

def multiply_chunks_safe(x_chunks, y_chunks):
    x_small = split_chunks(x_chunks, SMALL_BASE, RATIO)
    y_small = split_chunks(y_chunks, SMALL_BASE, RATIO)

    raw_small = fft_convolve(x_small, y_small)
    normalized_small = apply_carry(raw_small, SMALL_BASE)

    big_chunks = join_chunks(normalized_small, SMALL_BASE, RATIO)
    return tuple(big_chunks)
```

### Альтернатива: NTT

Теоретико-числовое преобразование (NTT) использует модульную арифметику вместо комплексных чисел — никаких float, всё точно. Но требует знания теории чисел. Для задачи на 10000 символов FFT с дроблением chunks вполне справится.

### Проверочные задачи к 4.2

1. Какова оценка $c_k^{max}$ при `n = 1024` и `BASE = 10000`? Помещается ли в float?
2. Если использовать `SMALL_BASE = 1000` и `RATIO = 1`, при каком внешнем `BASE` это означает работу без разбиения?
3. Почему `round` работает? Что должно быть верно про ошибку $\varepsilon$?

---

## 4.3. Сборка финального кода

```python
import cmath

BASE = 10000
SMALL_BASE = 100
RATIO = 2


def fft(a, invert=False):
    n = len(a)
    if n == 1:
        return list(a)

    a_even = a[0::2]
    a_odd  = a[1::2]

    e = fft(a_even, invert)
    o = fft(a_odd,  invert)

    angle_sign = -1 if invert else 1
    result = [0] * n
    for k in range(n // 2):
        angle = angle_sign * 2 * cmath.pi * k / n
        w = cmath.exp(1j * angle)
        t = w * o[k]
        result[k]          = e[k] + t
        result[k + n // 2] = e[k] - t

    return result


def ifft(a):
    n = len(a)
    result = fft(a, invert=True)
    return [x / n for x in result]


def split_chunks(chunks, small_base, ratio):
    result = []
    for c in chunks:
        for _ in range(ratio):
            result.append(c % small_base)
            c //= small_base
    return result


def join_chunks(small_chunks, small_base, ratio):
    result = []
    i = 0
    while i < len(small_chunks):
        c = 0
        multiplier = 1
        for j in range(ratio):
            if i + j < len(small_chunks):
                c += small_chunks[i + j] * multiplier
            multiplier *= small_base
        result.append(c)
        i += ratio
    return result


def fft_convolve(a, b):
    result_size = len(a) + len(b) - 1
    n = 1
    while n < result_size:
        n *= 2

    a_padded = list(a) + [0] * (n - len(a))
    b_padded = list(b) + [0] * (n - len(b))

    fa = fft(a_padded)
    fb = fft(b_padded)

    fc = [fa[i] * fb[i] for i in range(n)]

    c_complex = ifft(fc)

    return [round(x.real) for x in c_complex[:result_size]]


def apply_carry(raw_chunks, base):
    result = []
    carry = 0
    for c in raw_chunks:
        total = c + carry
        result.append(total % base)
        carry = total // base

    while carry > 0:
        result.append(carry % base)
        carry //= base

    return tuple(result)


def multiply_chunks(x_chunks, y_chunks):
    x_small = split_chunks(x_chunks, SMALL_BASE, RATIO)
    y_small = split_chunks(y_chunks, SMALL_BASE, RATIO)

    raw_small = fft_convolve(x_small, y_small)
    normalized_small = apply_carry(raw_small, SMALL_BASE)

    return tuple(join_chunks(normalized_small, SMALL_BASE, RATIO))


def multiplication(bigf1, bigf2):
    new_sign = bigf1.sign * bigf2.sign
    new_exponent = bigf1.exponent + bigf2.exponent
    new_chunks = multiply_chunks(bigf1.chunks, bigf2.chunks)
    return normalize(BigFloat(new_sign, new_chunks, new_exponent))
```

### Ручная проверка

Умножим $X = 1234.56$ на $Y = 7.89$. Ответ: $9740.6784$.

В BigFloat:
- $X$: chunks=(3456, 12), exp=-2.
- $Y$: chunks=(789,), exp=-2.

new_exponent = -4. Свёртка `(3456, 12)` и `(789,)`:
- $c_0 = 3456 \cdot 789 = 2{,}726{,}784$
- $c_1 = 12 \cdot 789 = 9{,}468$

raw = `[2726784, 9468]`. apply_carry с `BASE = 10000`:
- $2{,}726{,}784 = 6784 + 272 \cdot 10000$ → кладём 6784, переносим 272.
- $9468 + 272 = 9740$ → кладём 9740.

chunks=(6784, 9740), exp=-4. Это $9740 \cdot 10000 + 6784 = 97{,}406{,}784$, делённое на $10^4$ = $9740.6784$. ✓

---

# Блок 5. Резерв и систематизация

## Что должно быть в голове

- **Зачем переходить в «значения».** Умножение поточечное, $O(n)$.
- **Зачем корни из единицы.** Перевод коэффициенты ↔ значения за $O(n \log n)$.
- **Зачем рекурсия пополам.** Формула $A(x) = A_{even}(x^2) + x \cdot A_{odd}(x^2)$ + сжатие сводят DFT длины $n$ к двум DFT длины $n/2$. Симметрия даёт вторую половину даром.
- **Зачем степень двойки.** Чтобы рекурсия дошла до базы.
- **Зачем дробление chunks.** Точность float при больших `BASE`.
- **Зачем carry.** Свёртка даёт $c_k > \text{BASE}$.

## Контрольные вопросы

1. Почему наивное умножение $O(n^2)$, а через FFT — $O(n \log n)$? Где выигрыш?
2. Что произойдёт, если в FFT использовать произвольные точки $0, 1, 2, \dots$ вместо корней? Будет ли алгоритм правильным? Быстрым?
3. Зачем дополнять нулями: (а) до $\geq n + m - 1$, (б) до степени двойки?
4. В чём разница между прямым и обратным FFT на уровне кода?
5. Что такое «бабочка» и почему экономит вдвое умножений?

## Готовность к коду

**Технические:**
- Понимать `cmath.exp(1j * angle)`.
- Уметь прокрутить FFT длины 4 на бумаге.
- Знать, где округление и почему оно безопасно.

**Архитектурные:**
- `fft_convolve` отделена от `multiply_chunks`.
- sign и exponent обрабатываются в `multiplication`, а не внутри.
- Дробление и склейка chunks — на входе/выходе `multiply_chunks`.

**Тестовые случаи:**
- $0 \cdot X = 0$, $1 \cdot X = X$, $X \cdot Y = Y \cdot X$.
- Большие числа (1000+ цифр) — сравнивать с `int` Python.
- Время на 10000 цифр < 1 секунды.

## Что дальше после умножения

- **Деление** через метод Ньютона: $z_{n+1} = z_n (2 - Y z_n)$.
- **Корень** через Ньютона: $z_{n+1} = \frac{1}{2}(z_n + X / z_n)$.

FFT-умножение — базовый кирпич для всего остального.

---

# Разборы всех задач

## Задачи к 1.1

### Задача 1. Перемножьте $A(x) = 1 + x + x^2$ и $B(x) = 2 + 3x$

$$(1 + x + x^2)(2 + 3x) = 2 + 3x + 2x + 3x^2 + 2x^2 + 3x^3 = 2 + 5x + 5x^2 + 3x^3$$

Коэффициенты: $(2, 5, 5, 3)$.

### Задача 2. Число 50000000 в системе с `BASE = 1000`

$50{,}000{,}000 = 50 \cdot 1000^2$. Chunks (little-endian): $(0, 0, 50)$.
- `chunks[0] = 0` → коэффициент при $x^0$
- `chunks[1] = 0` → коэффициент при $x^1$
- `chunks[2] = 50` → коэффициент при $x^2$

Многочлен: $A(x) = 50 x^2$. Проверка: $A(1000) = 50 \cdot 1000^2 = 50{,}000{,}000$. ✓

### Задача 3. Сколько коэффициентов у произведения

Если $A$ имеет $n = 4$ коэффициентов (степень 3), $B$ имеет $m = 6$ коэффициентов (степень 5), то у произведения степень $3 + 5 = 8$, число коэффициентов $n + m - 1 = 9$.

Старший член $A$: $a_3 x^3$. Старший $B$: $b_5 x^5$. Их произведение: $a_3 b_5 \cdot x^8$. Степени идут от $x^0$ до $x^8$ — это 9 значений.

**Проверка на задаче 1:** $A$ имел 3 коэффициента, $B$ имел 2. Должно быть $3 + 2 - 1 = 4$ — действительно $(2, 5, 5, 3)$. ✓

---

## Задачи к 1.2

### Задача 1. $z = 2 + 2i$ в показательной форме

Модуль: $r = \sqrt{2^2 + 2^2} = \sqrt{8} = 2\sqrt{2}$.

Аргумент: точка $(2, 2)$ в первой четверти, координаты равны → $\varphi = 45° = \pi/4$. Формально: $\tan \varphi = 2/2 = 1$, $\varphi = \arctan(1) = \pi/4$.

$$z = 2\sqrt{2} \cdot e^{i\pi/4}$$

**Проверка.** $e^{i\pi/4} = \cos(\pi/4) + i \sin(\pi/4) = \frac{\sqrt{2}}{2} + i \frac{\sqrt{2}}{2}$. Умножаем на $2\sqrt{2}$: $\frac{2\sqrt{2} \cdot \sqrt{2}}{2} + i \frac{2\sqrt{2} \cdot \sqrt{2}}{2} = 2 + 2i$. ✓

### Задача 2. $i \cdot i$ двумя способами

**(а) Декартово.** $i \cdot i = i^2 = -1$ (по определению).

**(б) Показательно.** $i = e^{i\pi/2}$ (проверка: $e^{i\pi/2} = \cos(\pi/2) + i\sin(\pi/2) = 0 + i = i$). 

Умножение в показательной форме — модули умножаются, аргументы складываются:

$$i \cdot i = e^{i\pi/2} \cdot e^{i\pi/2} = e^{i\pi}$$

$e^{i\pi} = \cos(\pi) + i\sin(\pi) = -1 + 0 = -1$.

Геометрически: два поворота по 90° дают 180°, что соответствует $-1$.

### Задача 3. Углы поворота

Умножение на $z = re^{i\varphi}$ — поворот на $\varphi$ + растяжение в $r$ раз. При $r=1$ — только поворот.

**$-1$.** Точка $(-1, 0)$, модуль 1, аргумент 180°. Поворот на **180°**.

Проверка: $5 \to -5$, $i \to -i$ — все точки отражаются через ноль.

**$i$.** $i = e^{i\pi/2}$, аргумент 90°. Поворот на **90°**.

Проверка: $1 \cdot i = i$ (точка $(1,0) \to (0,1)$), $i \cdot i = -1$ (точка $(0,1) \to (-1,0)$), и так далее. Четыре умножения возвращают в исходную точку.

---

## Задачи к 1.3

### Задача 1. Все корни 6-й степени

$\omega_6^k = e^{i\pi k / 3}$, углы с шагом 60°.

| $k$ | Угол | Значение |
|---|---|---|
| 0 | 0° | $1$ |
| 1 | 60° | $\frac{1}{2} + i\frac{\sqrt{3}}{2}$ |
| 2 | 120° | $-\frac{1}{2} + i\frac{\sqrt{3}}{2}$ |
| 3 | 180° | $-1$ |
| 4 | 240° | $-\frac{1}{2} - i\frac{\sqrt{3}}{2}$ |
| 5 | 300° | $\frac{1}{2} - i\frac{\sqrt{3}}{2}$ |

Проверка: $\omega_6^0$ и $\omega_6^3$ отличаются знаком (свойство симметрии при $k=0$). $\omega_6^1$ и $\omega_6^4$ тоже отличаются знаком.

### Задача 2. Симметрия для $n = 8, k = 1$

$\omega_8^1 = e^{i\pi/4} = \cos(45°) + i\sin(45°) = \frac{\sqrt{2}}{2} + i\frac{\sqrt{2}}{2}$.

$\omega_8^5 = e^{i \cdot 5\pi/4}$. Угол 225° = 180° + 45°, в третьей четверти:
- $\cos(225°) = -\frac{\sqrt{2}}{2}$
- $\sin(225°) = -\frac{\sqrt{2}}{2}$

$\omega_8^5 = -\frac{\sqrt{2}}{2} - i\frac{\sqrt{2}}{2}$.

Проверка: $-\omega_8^1 = -\frac{\sqrt{2}}{2} - i\frac{\sqrt{2}}{2} = \omega_8^5$. ✓

### Задача 3. Значения $\omega_8^{2k}$ при $k = 0, \dots, 7$

По свойству сжатия: $\omega_8^{2k} = \omega_4^k$. По периодичности при $k \geq 4$: $\omega_4^{k+4} = \omega_4^k$.

| $k$ | $2k \bmod 8$ | $\omega_8^{2k}$ |
|---|---|---|
| 0 | 0 | $1$ |
| 1 | 2 | $i$ |
| 2 | 4 | $-1$ |
| 3 | 6 | $-i$ |
| 4 | 0 | $1$ |
| 5 | 2 | $i$ |
| 6 | 4 | $-1$ |
| 7 | 6 | $-i$ |

**4 различных значения**: $1, i, -1, -i$. Каждое повторяется дважды. Это все корни 4-й степени из единицы.

---

## Задачи к 2.1

### Задача 1. $A(x) = 1 + 2x + x^2$ в значениях в точках $-1, 0, 1$

- $A(-1) = 1 - 2 + 1 = 0$
- $A(0) = 1$
- $A(1) = 1 + 2 + 1 = 4$

Значения: $(0, 1, 4)$.

### Задача 2. $B$ степени ≤ 1, $B(0) = 5, B(2) = 11$

$B(x) = b_0 + b_1 x$. Уравнения:
- $B(0) = b_0 = 5$
- $B(2) = b_0 + 2b_1 = 11$

Из первого $b_0 = 5$. Подставляем: $5 + 2b_1 = 11 \Rightarrow b_1 = 3$.

$B(x) = 5 + 3x$.

### Задача 3. Сколько точек для произведения 100 × 100

Сами $A$ и $B$: по 100 коэффициентов, нужно **минимум 100 точек** на каждый.

Произведение $C = A \cdot B$: $100 + 100 - 1 = 199$ коэффициентов. Нужно **минимум 199 точек** для всех трёх (важно — в одних и тех же точках).

На практике для FFT берут ближайшую сверху степень двойки от 199 — это 256.

---

## Задачи к 2.2

### Задача 1. DFT для $n = 4$ как 4 уравнения

Корни: $\omega_4^0 = 1, \omega_4^1 = i, \omega_4^2 = -1, \omega_4^3 = -i$.

$\hat{a}_k = \sum_{j=0}^{3} a_j \omega_4^{jk}$:

$$\hat{a}_0 = a_0 + a_1 + a_2 + a_3$$
$$\hat{a}_1 = a_0 + a_1 \cdot i + a_2 \cdot (-1) + a_3 \cdot (-i) = a_0 + a_1 i - a_2 - a_3 i$$
$$\hat{a}_2 = a_0 + a_1 \cdot (-1) + a_2 \cdot 1 + a_3 \cdot (-1) = a_0 - a_1 + a_2 - a_3$$
$$\hat{a}_3 = a_0 + a_1 \cdot (-i) + a_2 \cdot (-1) + a_3 \cdot i = a_0 - a_1 i - a_2 + a_3 i$$

Замечание: значения в нулевой строке — просто сумма всех коэффициентов (потому что $\omega_4^0 = 1$). Это всегда так: $\hat{a}_0 = \sum a_j$.

### Задача 2. DFT $A(x) = 1 + x$ длины 2

$n = 2$, $\omega_2 = e^{i\pi} = -1$. Корни: $\omega_2^0 = 1, \omega_2^1 = -1$.

$\hat{a}_0 = a_0 + a_1 \cdot 1 = 1 + 1 = 2$.

$\hat{a}_1 = a_0 + a_1 \cdot (-1) = 1 - 1 = 0$.

Значения DFT: $(2, 0)$.

### Задача 3. Почему в лоб $O(n^2)$

Алгоритм «в лоб»: для каждого $k$ вычислять $A(\omega_n^k) = \sum_j a_j (\omega_n^k)^j$ независимо через схему Горнера за $O(n)$. Получается $n$ независимых вычислений по $O(n)$ каждое — итого $O(n^2)$.

Что не используется: тот факт, что **значения в разных точках связаны через свойства корней** (симметрия, сжатие). Алгоритм в лоб обращается с $\omega_n^k$ как с произвольными числами и пересчитывает каждое значение с нуля. FFT же экономит на том, что часть промежуточных вычислений переиспользуется между $\hat{a}_k$ и $\hat{a}_{k+n/2}$ (бабочки).

---

## Задачи к 3.1

### Задача 1. Уровни рекурсии для $n = 8$

$8 \to 4 \to 2 \to 1$. Делим **3 раза**, после чего попадаем в базовый случай.

Уровней рекурсии: 3 (плюс базовый уровень — итого 4 уровня в дереве вызовов).

В общем виде: $\log_2 n$ делений. Для $n = 1024$ — 10 уровней. Эта величина и определяет $\log n$ множитель в общей сложности.

### Задача 2. Шаг рекурсии для $n = 4$

$A(x) = a_0 + a_1 x + a_2 x^2 + a_3 x^3$.

Разделение:
- $A_{even}(y) = a_0 + a_2 y$
- $A_{odd}(y) = a_1 + a_3 y$

DFT $A_{even}$ длины 2 в корнях $\omega_2^0 = 1, \omega_2^1 = -1$:
- $\hat{e}_0 = a_0 + a_2$
- $\hat{e}_1 = a_0 - a_2$

DFT $A_{odd}$:
- $\hat{o}_0 = a_1 + a_3$
- $\hat{o}_1 = a_1 - a_3$

Корни $\omega_4$: $\omega_4^0 = 1, \omega_4^1 = i$ (для верхней половины используем симметрию).

Бабочки для $k = 0, 1$:
$$\hat{a}_0 = \hat{e}_0 + \omega_4^0 \cdot \hat{o}_0 = (a_0 + a_2) + 1 \cdot (a_1 + a_3) = a_0 + a_1 + a_2 + a_3$$
$$\hat{a}_2 = \hat{e}_0 - \omega_4^0 \cdot \hat{o}_0 = (a_0 + a_2) - (a_1 + a_3) = a_0 - a_1 + a_2 - a_3$$
$$\hat{a}_1 = \hat{e}_1 + \omega_4^1 \cdot \hat{o}_1 = (a_0 - a_2) + i(a_1 - a_3) = a_0 + i a_1 - a_2 - i a_3$$
$$\hat{a}_3 = \hat{e}_1 - \omega_4^1 \cdot \hat{o}_1 = (a_0 - a_2) - i(a_1 - a_3) = a_0 - i a_1 - a_2 + i a_3$$

Сравните с задачей 2.2.1 — совпадает в точности. ✓

### Задача 3. Без свойства симметрии

Без $\omega_n^{k+n/2} = -\omega_n^k$ нельзя было бы получить вторую половину $\hat{a}$ через простую смену знака. Пришлось бы для $k$ из $n/2..n-1$ вычислять $\hat{a}_k$ отдельно — то есть отдельно вызывать $A_{even}(\omega_{n/2}^k)$ для этих $k$, что удваивало бы стоимость склейки до $O(n^2 / n) \cdot 2 = O(n)$ — уже не критично.

Но если бы заодно сломалось свойство сжатия (не было бы $\omega_n^{2k} = \omega_{n/2}^k$), то рекурсивный вызов FFT не имел бы смысла — пришлось бы вычислять $A_{even}$ в произвольных точках, что не сводится к стандартному DFT и стоит $O(n^2)$.

Главное — без свойства симметрии теряется важный фактор «вдвое меньше умножений на склейке». Без свойства сжатия рекурсия вообще невозможна.

---

## Задачи к 3.2

### Задача 1. Без базового случая

Если убрать `if n == 1: return a`, рекурсия будет вызывать сама себя для всё меньших размеров: $n=2 \to n=1 \to n=0 \to ?$. При $n=1$ функция попытается разделить массив на чётные и нечётные — `a_even = [a[0]]`, `a_odd = []` — и рекурсивно вызвать FFT для пустого списка. Внутри `n // 2 = 0`, цикл не выполнится, но рекурсия будет вызвана для пустого массива бесконечно (или до RecursionError в Python).

На практике — `RecursionError: maximum recursion depth exceeded` или некорректный пустой результат, в зависимости от того, как именно сломается.

### Задача 2. Зачем `c_complex[:result_size]`

После FFT длина массива — $n$, ближайшая сверху степень двойки от $\text{len}(a) + \text{len}(b) - 1$. Например, для $\text{len}(a) = \text{len}(b) = 2$ имеем $\text{result\_size} = 3$, а $n = 4$.

После обратного FFT мы получаем массив длины 4. Первые 3 элемента — реальные коэффициенты $C$. Четвёртый — должен быть нулевым (потому что $C$ имеет степень 2 и коэффициента при $x^3$ нет), но из-за ошибок float может быть `0.0000001`. После округления — `0`.

Технически массив с хвостовым нулём представляет тот же многочлен. Но логически нужно вернуть ровно столько коэффициентов, сколько у $C$ — иначе будут лишние «значащие» нули в `chunks`. Поэтому обрезаем до `result_size`.

### Задача 3. Алгоритм для $A(x) = 1, B(x) = 1 + x$

`len(a) = 1, len(b) = 2, result_size = 1 + 2 - 1 = 2`. Ближайшая степень двойки $\geq 2$ — это $n = 2$.

Паддинг: `a_padded = [1, 0]`, `b_padded = [1, 1]`.

FFT $a$ длины 2: $\hat{a}_0 = 1 + 0 = 1, \hat{a}_1 = 1 - 0 = 1$. Значит, $\hat{a} = (1, 1)$.

FFT $b$: $\hat{b}_0 = 1 + 1 = 2, \hat{b}_1 = 1 - 1 = 0$. $\hat{b} = (2, 0)$.

Поточечно: $\hat{c} = (2, 0)$.

Обратное FFT: $c_j = \frac{1}{2} \sum_k \hat{c}_k \omega_2^{-jk}$. 
- $c_0 = \frac{1}{2}(2 \cdot 1 + 0 \cdot 1) = 1$
- $c_1 = \frac{1}{2}(2 \cdot 1 + 0 \cdot (-1)) = 1$

$c = (1, 1)$, что соответствует $C(x) = 1 + x$. ✓

---

## Задачи к 4.1

### Задача 1. $X = 12, Y = 13$ при `BASE = 10`

Chunks (little-endian):
- $X = 12$: chunks $(2, 1)$.
- $Y = 13$: chunks $(3, 1)$.

Свёртка вручную ($c_k = \sum_{i+j=k} a_i b_j$):
- $c_0 = 2 \cdot 3 = 6$
- $c_1 = 2 \cdot 1 + 1 \cdot 3 = 5$
- $c_2 = 1 \cdot 1 = 1$

raw = `[6, 5, 1]`.

`apply_carry` с base=10: каждое значение уже в `[0, 10)`, переносов нет.
result = `(6, 5, 1)`.

Проверка: $6 + 5 \cdot 10 + 1 \cdot 100 = 156$. И $12 \cdot 13 = 156$. ✓

### Задача 2. `raw = [0, 0, 200]` при `BASE = 100`

Применяем carry:
- `c=0, total=0+0=0`, кладём 0, carry=0.
- `c=0, total=0+0=0`, кладём 0, carry=0.
- `c=200, total=200+0=200`, кладём $200 \bmod 100 = 0$, carry $= 200 // 100 = 2$.

После цикла carry=2 > 0, добавляем: $2 \bmod 100 = 2$, carry становится 0.

result = `(0, 0, 0, 2)`.

Проверка: $0 + 0 \cdot 100 + 0 \cdot 100^2 + 2 \cdot 100^3 = 2{,}000{,}000$. А raw представлял $0 + 0 \cdot 100 + 200 \cdot 10000 = 2{,}000{,}000$. ✓

### Задача 3. Сложение экспонент

$X = 12.5$ имеет chunks=(125,), exp=-1. То есть $X = 125 \cdot 10^{-1} = 12.5$.

$Y = 0.04$ имеет chunks=(4,), exp=-2. То есть $Y = 4 \cdot 10^{-2} = 0.04$.

$X \cdot Y = 12.5 \cdot 0.04 = 0.5$.

Если складываем экспоненты: new_exp = $-1 + (-2) = -3$. Свёртка chunks: $125 \cdot 4 = 500$. Результат: chunks=(500,), exp=-3, что значит $500 \cdot 10^{-3} = 0.5$. ✓

Алгебраически: $X = 125 \cdot 10^{-1}$, $Y = 4 \cdot 10^{-2}$, тогда $X \cdot Y = (125 \cdot 4) \cdot 10^{-1+(-2)} = 500 \cdot 10^{-3}$. Экспоненты при умножении степеней с одинаковым основанием складываются — стандартное правило.

---

## Задачи к 4.2

### Задача 1. $c_k^{max}$ при `n = 1024, BASE = 10000`

$c_k^{max} = n \cdot \text{BASE}^2 = 1024 \cdot 10^8 \approx 1.024 \cdot 10^{11}$.

Это около 12 десятичных цифр. У float (double) точность ~15-16 значащих цифр, остаётся 3-4 цифры запаса на ошибку. На грани, но обычно работает — можно тестировать. Для надёжности лучше уменьшить `BASE` до $10^3$ или $10^2$.

### Задача 2. `SMALL_BASE = 1000, RATIO = 1`

При `RATIO = 1` каждый chunk превращается в один внутренний chunk. Это работает без разбиения только когда внешний и внутренний `BASE` совпадают: внешний `BASE = SMALL_BASE = 1000`.

Если внешний `BASE = 10000`, а внутренний `1000` с RATIO=1, то `split_chunks` положит `c % 1000` (потеряв старшие цифры), и информация будет утеряна. Чтобы внешний 10000 корректно разбить на внутренние 1000, нужен RATIO=2 (потому что $10000 = 1000 \cdot 10$, неудобный коэффициент — фактически на практике берут SMALL_BASE-степень внешнего, например $10000 = 100^2$).

### Задача 3. Почему `round` работает

Если истинное $c_k$ — целое, а float-результат это $c_k + \varepsilon$, то `round` вернёт правильный $c_k$ при условии $|\varepsilon| < 0.5$.

Чтобы это гарантировать, абсолютная ошибка float после всех операций FFT должна быть меньше 0.5. Анализ показывает, что ошибка масштабируется как $\varepsilon \sim n \cdot \text{BASE}^2 \cdot 2^{-52}$ (где $2^{-52}$ — относительная точность double).

Подставляя `n = 4096, BASE = 100`: $\varepsilon \sim 4096 \cdot 10^4 \cdot 2 \cdot 10^{-16} \approx 10^{-8}$ — на много порядков меньше 0.5. Округление безопасно.

Для `BASE = 10000`: $\varepsilon \sim 4096 \cdot 10^8 \cdot 2 \cdot 10^{-16} \approx 10^{-4}$ — всё ещё меньше 0.5, но запас гораздо меньше, и при «неудачных» данных может расти. Поэтому дробление chunks — правильная мера предосторожности.
