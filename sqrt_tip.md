# Вычисление квадратного корня

---

## Зачем вообще считать корень

В квадратном уравнении `ax² + bx + c = 0` есть формула:

```
x = (-b ± √D) / 2a
где D = b² - 4ac
```

`D` - это огромное целое число (может быть тысячи цифр). Нужно найти √D **точно**, без погрешностей. Обычный `math.sqrt()` не подойдёт - он работает с дробными числами и теряет точность.

---

## Главная идея - целочисленный корень

Программа ищет не `2.2360679...`, а **целую часть** корня. То есть такое целое число `root`, что:

```
root² ≤ n < (root+1)²
```

Например для `n = 17`:
```
4² = 16 ≤ 17 < 25 = 5²
→ root = 4
```

Если `root² = n` точно — корень целый, ответ красивый. Если нет — выводим символьно `√17`.

---

## Алгоритм 1 - Метод Ньютона (маленькие числа)

### Откуда берётся формула

Представь что ищешь √n. Начинаешь с любого угаданного числа `x`. Если `x` слишком большой - то `n/x` будет слишком маленьким. Истинный корень где-то **между** ними. Берём середину:

```
x_новый = (x + n/x) / 2
```

Это и есть итерация Ньютона. Повторяем пока не сойдётся.

### Пример - √2 с нуля

```
n = 2, начинаем с x₀ = 1

x₁ = (1 + 2/1) / 2     = 1.5
x₂ = (1.5 + 2/1.5) / 2 = 1.4166...
x₃ = (1.4166 + 2/1.4166) / 2 = 1.41421...
x₄ = 1.41421356...
```

Каждый шаг - вдвое больше верных цифр. Для 1000-значного числа нужно около 10 итераций.

### Когда останавливаться

Когда два соседних результата стали одинаковыми:

```
если x_новый >= x_старый → стоп, x_старый и есть ответ
```

Это значит точность исчерпана - дальше улучшать некуда.

### Начальное приближение

Чем лучше начальное приближение - тем меньше итераций. Программа берёт грубую оценку через длину числа:

```
n имеет k цифр → √n имеет примерно k/2 цифр
→ начинаем с 10^(k/2)
```

---

## Алгоритм 2 - Рекурсия (большие числа)

### Проблема метода Ньютона на больших числах

Каждая итерация требует деления. Деление больших чисел само по себе медленное. При тысячах итераций это становится очень долго.

### Идея - разбей число пополам

Представь число `n` с 1000 цифрами. Разбиваем его на две половины:

```
n = [первые 500 цифр] [последние 500 цифр]
      старшая часть     младшая часть
```

Корень от старшей части примерно равен `√n / 10²⁵⁰` - это грубое приближение, но уже близкое к ответу.

### Пример простыми числами

```
n = 1234567890

старшая половина = 12345  (первые 5 цифр)
√12345 ≈ 111

масштабируем обратно:
111 × 10^(5/2) ≈ 11100  ← грубое приближение √n
```

Несколько итераций Ньютона от этой точки - и получаем точный ответ. Итераций нужно мало, потому что начальное приближение уже хорошее.

### Рекурсия

Но как взять корень от старшей половины? Точно так же - разбить её пополам и взять корень рекурсивно. И так далее, пока число не станет маленьким:

```
√(1000 цифр)
    ↓
√(500 цифр) → уточнить
    ↓
√(250 цифр) → уточнить
    ↓
√(125 цифр) → уточнить
    ↓
√(62 цифры) → метод Ньютона напрямую  ← база рекурсии
```

Снизу вверх каждый уровень берёт результат от предыдущего и уточняет несколькими итерациями.

---

## Финальная проверка

После всех вычислений результат может ошибиться на ±1 из-за округлений при делении. Поэтому в самом конце явная проверка:

```
вычислили root

проверяем:
root² ≤ n ?        если нет → root был слишком большой → берём root - 1
(root+1)² > n ?    если нет → root был слишком маленький → берём root + 1
всё верно ?        → root готов
```

Это дешёвая операция - одно-два умножения - но гарантирует абсолютную точность.

---

## Что происходит в солвере после

```
вычислили root = √D

root * root == D ?
    да  → корень точный, пишем красивую дробь:
          x = (-b + root) / 2a

    нет → корень иррациональный, пишем символьно:
          x = (-b + √D) / 2a
```

---

## Итог одной схемой

```
n маленькое (< 9 чанков)
    → метод Ньютона
    → итерации x = (x + n/x) / 2
    → стоп когда x не улучшается

n большое (≥ 9 чанков)
    → разбить пополам
    → рекурсивно взять корень от старшей половины
    → уточнить итерациями Ньютона
    → финальная проверка ±1

результат
    → точный целый   → красивая дробь в ответе
    → нецелой        → символьная запись √D
```
