# Быстрое преобразование Фурье (БПФ / FFT)

> Перевод статьи с сайта [cp-algorithms.com](https://cp-algorithms.com/algebra/fft.html). Оригинал распространяется под лицензией [Creative Commons Attribution Share Alike 4.0 International](https://github.com/cp-algorithms/cp-algorithms/blob/main/LICENSE).

## Введение

В этой статье мы обсудим алгоритм, который позволяет умножать два многочлена длины $n$ за время $O(n \log n)$ — это значительно быстрее тривиального умножения за $O(n^2)$.

Очевидно, что умножение двух длинных чисел также сводится к умножению многочленов, поэтому два длинных числа можно перемножать за $O(n \log n)$ (где $n$ — количество цифр).

**Открытие быстрого преобразования Фурье (БПФ, FFT — Fast Fourier Transform)** обычно приписывают Кули и Тьюки, которые опубликовали свой алгоритм в 1965 году. На самом деле БПФ открывали неоднократно и до них, но важность этого открытия не была осознана до появления современных компьютеров. Некоторые исследователи приписывают открытие БПФ Рунге и Кёнигу (1924). А ещё раньше, в 1805 году, аналогичный метод разработал Гаусс — но не опубликовал его.

Обратите внимание: представленный здесь алгоритм БПФ работает за $O(n \log n)$, но он **не подходит** для умножения произвольно больших многочленов с произвольно большими коэффициентами или для умножения произвольно больших целых чисел. Он легко справляется с многочленами размера $10^5$ с небольшими коэффициентами, или с умножением двух чисел размера $10^6$, чего обычно достаточно для решения задач спортивного программирования. При умножении чисел длиннее $10^6$ бит точности и диапазона чисел с плавающей точкой уже не хватает для получения точного результата, хотя существуют более сложные модификации алгоритма.

Например, в 1971 году Шёнхаге и Штрассен разработали вариант для умножения произвольно больших чисел, рекурсивно применяющий БПФ в кольцевых структурах, со сложностью $O(n \log n \log \log n)$. А недавно (2019) Харви и ван дер Ховен опубликовали алгоритм с настоящей сложностью $O(n \log n)$.

---

## Дискретное преобразование Фурье (ДПФ)

Пусть дан многочлен степени $n - 1$:

$$A(x) = a_0 x^0 + a_1 x^1 + \dots + a_{n-1} x^{n-1}$$

Без ограничения общности будем считать, что $n$ — количество коэффициентов — является степенью двойки. Если это не так, мы добавляем недостающие члены $a_i x^i$ с коэффициентами $a_i = 0$.

Из теории комплексных чисел известно, что уравнение $x^n = 1$ имеет $n$ комплексных корней (называемых корнями $n$-й степени из единицы), и эти корни имеют вид $w_{n, k} = e^{\frac{2 k \pi i}{n}}$, где $k = 0, \dots, n-1$.

У этих чисел есть важные свойства: например, главный корень $w_n = w_{n, 1} = e^{\frac{2 \pi i}{n}}$ позволяет описать все остальные корни: $w_{n, k} = (w_n)^k$.

**Дискретным преобразованием Фурье (ДПФ)** многочлена $A(x)$ (или, что то же самое, вектора коэффициентов $(a_0, a_1, \dots, a_{n-1})$) называется вектор значений многочлена в точках $x = w_{n, k}$:

$$\begin{align}
\text{DFT}(a_0, a_1, \dots, a_{n-1}) &= (y_0, y_1, \dots, y_{n-1}) \\
&= (A(w_{n, 0}), A(w_{n, 1}), \dots, A(w_{n, n-1})) \\
&= (A(w_n^0), A(w_n^1), \dots, A(w_n^{n-1}))
\end{align}$$

Аналогично определяется **обратное дискретное преобразование Фурье**: обратное ДПФ от значений многочлена $(y_0, y_1, \dots, y_{n-1})$ — это коэффициенты многочлена $(a_0, a_1, \dots, a_{n-1})$.

$$\text{InverseDFT}(y_0, y_1, \dots, y_{n-1}) = (a_0, a_1, \dots, a_{n-1})$$

Таким образом, прямое ДПФ вычисляет значения многочлена в корнях $n$-й степени из единицы, а обратное ДПФ восстанавливает коэффициенты многочлена по этим значениям.

### Применение ДПФ: быстрое умножение многочленов

Пусть даны два многочлена $A$ и $B$. Вычислим их ДПФ: $\text{DFT}(A)$ и $\text{DFT}(B)$.

Что произойдёт, если мы перемножим эти многочлены? Очевидно, что в каждой точке значения просто умножаются:

$$(A \cdot B)(x) = A(x) \cdot B(x).$$

Это означает: если мы перемножим вектора $\text{DFT}(A)$ и $\text{DFT}(B)$ — поэлементно, — то получим в точности ДПФ произведения многочленов:

$$\text{DFT}(A \cdot B) = \text{DFT}(A) \cdot \text{DFT}(B)$$

И, применив обратное ДПФ, получаем:

$$A \cdot B = \text{InverseDFT}(\text{DFT}(A) \cdot \text{DFT}(B))$$

Здесь под произведением двух ДПФ подразумевается поэлементное произведение векторов. Оно вычисляется за $O(n)$. Если мы умеем вычислять ДПФ и обратное ДПФ за $O(n \log n)$, то и произведение многочленов (а значит, и длинных чисел) можно вычислить за то же время.

Важно: оба многочлена должны иметь одинаковую степень, иначе вектора ДПФ будут разной длины. Этого можно добиться, дополнив более короткий вектор нулями.

Кроме того, поскольку результат произведения двух многочленов имеет степень $2(n - 1)$, нам надо удвоить степень каждого исходного многочлена (опять же, дополняя нулями). Из вектора длины $n$ невозможно восстановить искомый многочлен с $2n - 1$ коэффициентами.

### Быстрое преобразование Фурье

**Быстрое преобразование Фурье** — это метод, позволяющий вычислять ДПФ за время $O(n \log n)$.

Основная идея БПФ — применить стратегию «разделяй и властвуй». Мы делим вектор коэффициентов многочлена на два вектора, рекурсивно вычисляем ДПФ для каждого из них, а затем объединяем результаты для получения ДПФ всего многочлена.

Пусть дан многочлен $A(x)$ степени $n - 1$, где $n$ — степень двойки и $n > 1$:

$$A(x) = a_0 x^0 + a_1 x^1 + \dots + a_{n-1} x^{n-1}$$

Разделим его на два многочлена меньшего размера: один содержит коэффициенты с чётными индексами, второй — с нечётными:

$$\begin{align}
A_0(x) &= a_0 x^0 + a_2 x^1 + \dots + a_{n-2} x^{\frac{n}{2}-1} \\
A_1(x) &= a_1 x^0 + a_3 x^1 + \dots + a_{n-1} x^{\frac{n}{2}-1}
\end{align}$$

Легко увидеть, что:

$$A(x) = A_0(x^2) + x \cdot A_1(x^2).$$

Многочлены $A_0$ и $A_1$ имеют вдвое меньше коэффициентов, чем $A$. Если мы умеем вычислять $\text{DFT}(A)$ за линейное время, имея $\text{DFT}(A_0)$ и $\text{DFT}(A_1)$, то получаем рекуррентное соотношение для времени работы:

$$T_{\text{DFT}}(n) = 2 T_{\text{DFT}}\left(\frac{n}{2}\right) + O(n),$$

что по основной теореме о рекурсии (master theorem) даёт $T_{\text{DFT}}(n) = O(n \log n)$.

Научимся это делать.

Предположим, мы вычислили вектора $\left(y_k^0\right)_{k=0}^{n/2-1} = \text{DFT}(A_0)$ и $\left(y_k^1\right)_{k=0}^{n/2-1} = \text{DFT}(A_1)$. Найдём выражение для $\left(y_k\right)_{k=0}^{n-1} = \text{DFT}(A)$.

Для первой половины ($\frac{n}{2}$ значений) можно использовать выведенное равенство $A(x) = A_0(x^2) + x A_1(x^2)$:

$$y_k = y_k^0 + w_n^k y_k^1, \quad k = 0, \dots, \frac{n}{2} - 1.$$

Для второй половины ($\frac{n}{2}$ значений) нужно вывести немного другую формулу:

$$\begin{align}
y_{k+n/2} &= A\left(w_n^{k+n/2}\right) \\
&= A_0\left(w_n^{2k+n}\right) + w_n^{k + n/2} A_1\left(w_n^{2k+n}\right) \\
&= A_0\left(w_n^{2k} w_n^n\right) + w_n^k w_n^{n/2} A_1\left(w_n^{2k} w_n^n\right) \\
&= A_0\left(w_n^{2k}\right) - w_n^k A_1\left(w_n^{2k}\right) \\
&= y_k^0 - w_n^k y_k^1
\end{align}$$

Здесь мы снова применили $A(x) = A_0(x^2) + x A_1(x^2)$ и тождества $w_n^n = 1$ и $w_n^{n/2} = -1$.

Итак, мы получили формулы для вычисления всего вектора $(y_k)$:

$$\begin{align}
y_k &= y_k^0 + w_n^k y_k^1, &\quad k = 0, \dots, \frac{n}{2} - 1, \\
y_{k+n/2} &= y_k^0 - w_n^k y_k^1, &\quad k = 0, \dots, \frac{n}{2} - 1.
\end{align}$$

(Этот шаблон $a + b$ и $a - b$ часто называют **«бабочкой»** — *butterfly*.)

Так мы научились вычислять ДПФ за $O(n \log n)$.

### Обратное БПФ

Пусть дан вектор $(y_0, y_1, \dots, y_{n-1})$ — значения многочлена $A$ степени $n - 1$ в точках $x = w_n^k$. Мы хотим восстановить коэффициенты $(a_0, a_1, \dots, a_{n-1})$ этого многочлена. Эта задача называется **интерполяцией**, и для неё существуют общие алгоритмы. Но в нашем частном случае (когда значения известны в корнях из единицы) можно получить гораздо более простой алгоритм, практически идентичный прямому БПФ.

Запишем ДПФ в матричной форме:

$$
\begin{pmatrix}
w_n^0 & w_n^0 & w_n^0 & w_n^0 & \cdots & w_n^0 \\
w_n^0 & w_n^1 & w_n^2 & w_n^3 & \cdots & w_n^{n-1} \\
w_n^0 & w_n^2 & w_n^4 & w_n^6 & \cdots & w_n^{2(n-1)} \\
w_n^0 & w_n^3 & w_n^6 & w_n^9 & \cdots & w_n^{3(n-1)} \\
\vdots & \vdots & \vdots & \vdots & \ddots & \vdots \\
w_n^0 & w_n^{n-1} & w_n^{2(n-1)} & w_n^{3(n-1)} & \cdots & w_n^{(n-1)(n-1)}
\end{pmatrix} \begin{pmatrix}
a_0 \\ a_1 \\ a_2 \\ a_3 \\ \vdots \\ a_{n-1}
\end{pmatrix} = \begin{pmatrix}
y_0 \\ y_1 \\ y_2 \\ y_3 \\ \vdots \\ y_{n-1}
\end{pmatrix}
$$

Эта матрица называется **матрицей Вандермонда**.

Вектор $(a_0, a_1, \dots, a_{n-1})$ можно получить, умножив вектор $(y_0, y_1, \dots, y_{n-1})$ слева на обратную матрицу:

$$
\begin{pmatrix}
a_0 \\ a_1 \\ a_2 \\ a_3 \\ \vdots \\ a_{n-1}
\end{pmatrix} = \begin{pmatrix}
w_n^0 & w_n^0 & \cdots & w_n^0 \\
w_n^0 & w_n^1 & \cdots & w_n^{n-1} \\
\vdots & \vdots & \ddots & \vdots \\
w_n^0 & w_n^{n-1} & \cdots & w_n^{(n-1)(n-1)}
\end{pmatrix}^{-1} \begin{pmatrix}
y_0 \\ y_1 \\ \vdots \\ y_{n-1}
\end{pmatrix}
$$

Несложной проверкой можно убедиться, что обратная матрица имеет вид:

$$
\frac{1}{n}
\begin{pmatrix}
w_n^0 & w_n^0 & w_n^0 & \cdots & w_n^0 \\
w_n^0 & w_n^{-1} & w_n^{-2} & \cdots & w_n^{-(n-1)} \\
w_n^0 & w_n^{-2} & w_n^{-4} & \cdots & w_n^{-2(n-1)} \\
\vdots & \vdots & \vdots & \ddots & \vdots \\
w_n^0 & w_n^{-(n-1)} & w_n^{-2(n-1)} & \cdots & w_n^{-(n-1)(n-1)}
\end{pmatrix}
$$

Получаем формулу:

$$a_k = \frac{1}{n} \sum_{j=0}^{n-1} y_j w_n^{-k j}$$

Сравним её с формулой для $y_k$:

$$y_k = \sum_{j=0}^{n-1} a_j w_n^{k j}.$$

Видно, что задачи практически одинаковы. Поэтому коэффициенты $a_k$ можно найти тем же алгоритмом «разделяй и властвуй», только вместо $w_n^k$ нужно использовать $w_n^{-k}$, а в конце разделить полученные коэффициенты на $n$.

Вычисление обратного ДПФ почти ничем не отличается от прямого и тоже выполняется за $O(n \log n)$.

### Реализация

Ниже приведена простая рекурсивная **реализация БПФ и обратного БПФ** в одной функции, поскольку различия минимальны. Для хранения комплексных чисел используем тип `complex` из STL C++.

```cpp
using cd = complex<double>;
const double PI = acos(-1);

void fft(vector<cd> & a, bool invert) {
    int n = a.size();
    if (n == 1)
        return;

    vector<cd> a0(n / 2), a1(n / 2);
    for (int i = 0; 2 * i < n; i++) {
        a0[i] = a[2*i];
        a1[i] = a[2*i+1];
    }
    fft(a0, invert);
    fft(a1, invert);

    double ang = 2 * PI / n * (invert ? -1 : 1);
    cd w(1), wn(cos(ang), sin(ang));
    for (int i = 0; 2 * i < n; i++) {
        a[i] = a0[i] + w * a1[i];
        a[i + n/2] = a0[i] - w * a1[i];
        if (invert) {
            a[i] /= 2;
            a[i + n/2] /= 2;
        }
        w *= wn;
    }
}
```

Функция получает вектор коэффициентов и записывает результат ДПФ (или обратного ДПФ) обратно в этот же вектор. Аргумент `invert` указывает, какое преобразование вычислять: прямое или обратное.

Внутри функции мы сначала проверяем, не равна ли длина вектора единице — в этом случае ничего делать не нужно. Иначе делим вектор $a$ на два — $a_0$ и $a_1$ — и рекурсивно считаем для них ДПФ. Затем инициализируем значение $wn$ и переменную $w$, которая будет содержать текущую степень $wn$. Далее по формулам выше вычисляем результат.

Если флаг `invert` установлен, то $wn$ заменяется на $wn^{-1}$, а каждое значение делится на $2$ (поскольку это происходит на каждом уровне рекурсии, в итоге результат поделится на $n$).

С помощью этой функции можно реализовать **умножение двух многочленов**:

```cpp
vector<int> multiply(vector<int> const& a, vector<int> const& b) {
    vector<cd> fa(a.begin(), a.end()), fb(b.begin(), b.end());
    int n = 1;
    while (n < a.size() + b.size()) 
        n <<= 1;
    fa.resize(n);
    fb.resize(n);

    fft(fa, false);
    fft(fb, false);
    for (int i = 0; i < n; i++)
        fa[i] *= fb[i];
    fft(fa, true);

    vector<int> result(n);
    for (int i = 0; i < n; i++)
        result[i] = round(fa[i].real());
    return result;
}
```

Эта функция работает с многочленами с целыми коэффициентами, но её можно адаптировать и под другие типы. Поскольку при работе с комплексными числами возникает вычислительная погрешность, в конце результат необходимо округлить.

Функция **умножения двух длинных чисел** практически не отличается от функции умножения многочленов. Единственное отличие — после умножения нужно нормализовать число (выполнить переносы):

```cpp
int carry = 0;
for (int i = 0; i < n; i++) {
    result[i] += carry;
    carry = result[i] / 10;
    result[i] %= 10;
}
```

Поскольку длина произведения двух чисел не превышает суммы их длин, размер вектора достаточен для всех переносов.

### Улучшенная реализация: вычисление «на месте»

Чтобы повысить эффективность, перейдём от рекурсивной реализации к итеративной. В рекурсивной версии мы явно разделяли вектор $a$ на два временных вектора. Однако если переупорядочить элементы определённым образом, можно обойтись без них — все вычисления будут выполняться **«на месте» (in-place)**, в самом векторе $A$.

Заметим, что на первом уровне рекурсии элементы, у которых младший бит индекса равен $0$, попадают в $a_0$, а у которых $1$ — в $a_1$. На втором уровне то же происходит со вторым по младшинству битом, и так далее. Поэтому если развернуть биты индекса каждого коэффициента и отсортировать по этим перевёрнутым значениям, мы получим нужный порядок (он называется **бит-реверсной перестановкой** — *bit-reversal permutation*).

Например, для $n = 8$ нужный порядок имеет вид:

$$a = \bigg\{ \Big[ (a_0, a_4), (a_2, a_6) \Big], \Big[ (a_1, a_5), (a_3, a_7) \Big] \bigg\}$$

И действительно, на первом уровне рекурсии (внешние фигурные скобки) вектор разделяется на две части: $[a_0, a_2, a_4, a_6]$ и $[a_1, a_3, a_5, a_7]$. В бит-реверсной перестановке это соответствует простому делению вектора пополам: первая половина и вторая половина. Затем рекурсивный вызов для каждой половины. Пусть результаты ДПФ возвращаются прямо вместо самих элементов:

$$a = \bigg\{ \Big[y_0^0, y_1^0, y_2^0, y_3^0\Big], \Big[y_0^1, y_1^1, y_2^1, y_3^1 \Big] \bigg\}$$

Теперь объединим эти два ДПФ в одно для всего вектора. Порядок элементов идеален, и объединение тоже можно выполнить непосредственно в этом векторе. Мы берём $y_0^0$ и $y_0^1$ и применяем «бабочку». Расположение результатов совпадает с расположением исходных значений:

$$a = \bigg\{ \Big[y_0^0 + w_n^0 y_0^1, y_1^0, y_2^0, y_3^0\Big], \Big[y_0^0 - w_n^0 y_0^1, y_1^1, y_2^1, y_3^1\Big] \bigg\}$$

Аналогично для остальных пар. В итоге:

$$a = \bigg\{ \Big[y_0^0 + w_n^0 y_0^1, y_1^0 + w_n^1 y_1^1, y_2^0 + w_n^2 y_2^1, y_3^0 + w_n^3 y_3^1\Big], \Big[y_0^0 - w_n^0 y_0^1, y_1^0 - w_n^1 y_1^1, y_2^0 - w_n^2 y_2^1, y_3^0 - w_n^3 y_3^1\Big] \bigg\}$$

Так мы вычислили требуемое ДПФ из вектора $a$.

Здесь описан только первый уровень рекурсии, но всё то же работает и для остальных. Таким образом, после применения бит-реверсной перестановки можно вычислить ДПФ «на месте», без дополнительной памяти.

Это также позволяет избавиться от рекурсии. Мы начинаем с самого нижнего уровня: делим вектор на пары и применяем «бабочку». Затем делим на блоки по 4 элемента и опять применяем «бабочку», получая ДПФ для каждого блока размера 4. И так далее. На последнем шаге мы получаем ДПФ обеих половин $a$ и, применяя «бабочку», получаем ДПФ всего вектора $a$.

```cpp
using cd = complex<double>;
const double PI = acos(-1);

int reverse(int num, int lg_n) {
    int res = 0;
    for (int i = 0; i < lg_n; i++) {
        if (num & (1 << i))
            res |= 1 << (lg_n - 1 - i);
    }
    return res;
}

void fft(vector<cd> & a, bool invert) {
    int n = a.size();
    int lg_n = 0;
    while ((1 << lg_n) < n)
        lg_n++;

    for (int i = 0; i < n; i++) {
        if (i < reverse(i, lg_n))
            swap(a[i], a[reverse(i, lg_n)]);
    }

    for (int len = 2; len <= n; len <<= 1) {
        double ang = 2 * PI / len * (invert ? -1 : 1);
        cd wlen(cos(ang), sin(ang));
        for (int i = 0; i < n; i += len) {
            cd w(1);
            for (int j = 0; j < len / 2; j++) {
                cd u = a[i+j], v = a[i+j+len/2] * w;
                a[i+j] = u + v;
                a[i+j+len/2] = u - v;
                w *= wlen;
            }
        }
    }

    if (invert) {
        for (cd & x : a)
            x /= n;
    }
}
```

Сначала применяется бит-реверсная перестановка путём обмена каждого элемента с элементом перевёрнутого индекса. Затем за $\log n - 1$ шагов вычисляется ДПФ для каждого блока соответствующего размера `len`. Для всех этих блоков один и тот же корень из единицы `wlen`. Мы перебираем все блоки и применяем «бабочку» к каждому из них.

Можно дополнительно оптимизировать инверсию битов. В предыдущей реализации мы перебирали все биты индекса и формировали инвертированный индекс. Однако можно делать это иначе.

Допустим, в $j$ уже хранится перевёрнутая запись $i$. Тогда при переходе к $i+1$ нужно увеличить $i$, а также увеличить $j$, но в «обращённой» системе счисления. Прибавление единицы в обычной двоичной системе — это инверсия всех младших единиц в нули и инверсия идущего за ними нуля в единицу. Эквивалентно, в «обращённой» системе мы инвертируем все старшие единицы и идущий за ними ноль.

Получаем такую реализацию:

```cpp
using cd = complex<double>;
const double PI = acos(-1);

void fft(vector<cd> & a, bool invert) {
    int n = a.size();

    for (int i = 1, j = 0; i < n; i++) {
        int bit = n >> 1;
        for (; j & bit; bit >>= 1)
            j ^= bit;
        j ^= bit;

        if (i < j)
            swap(a[i], a[j]);
    }

    for (int len = 2; len <= n; len <<= 1) {
        double ang = 2 * PI / len * (invert ? -1 : 1);
        cd wlen(cos(ang), sin(ang));
        for (int i = 0; i < n; i += len) {
            cd w(1);
            for (int j = 0; j < len / 2; j++) {
                cd u = a[i+j], v = a[i+j+len/2] * w;
                a[i+j] = u + v;
                a[i+j+len/2] = u - v;
                w *= wlen;
            }
        }
    }

    if (invert) {
        for (cd & x : a)
            x /= n;
    }
}
```

Кроме того, бит-реверсную перестановку можно предвычислить заранее. Это особенно полезно, когда размер $n$ один и тот же для всех вызовов. Но даже когда у нас всего три вызова (как при умножении двух многочленов), эффект заметен. Также можно предвычислить все корни из единицы и их степени.

---

## Теоретико-числовое преобразование (NTT)

Немного изменим задачу. Мы по-прежнему хотим перемножать два многочлена за $O(n \log n)$, но теперь хотим вычислять коэффициенты по модулю некоторого простого числа $p$. Конечно, можно использовать обычное ДПФ и взять остаток в конце. Но это может привести к ошибкам округления, особенно при больших числах.

**Теоретико-числовое преобразование (NTT — Number Theoretic Transform)** имеет преимущество: оно работает только с целыми числами, и потому результат гарантированно точный.

ДПФ основано на комплексных числах и корнях $n$-й степени из единицы. Для эффективного вычисления мы активно используем свойства этих корней (например, что один корень порождает все остальные через возведение в степень).

Те же свойства верны и для корней $n$-й степени из единицы в модулярной арифметике. **Корнем $n$-й степени из единицы по модулю $p$** называется такое число $w_n$, что:

$$\begin{align}
(w_n)^n &\equiv 1 \pmod{p}, \\
(w_n)^k &\not\equiv 1 \pmod{p}, \quad 1 \le k < n.
\end{align}$$

Остальные $n-1$ корней получаются как степени $w_n$.

Чтобы применить такие корни в БПФ, нам нужно, чтобы корень существовал для некоторого $n$, являющегося степенью двойки, и для всех меньших степеней двойки. Заметим интересное свойство:

$$\begin{align}
(w_n^2)^m = w_n^n &\equiv 1 \pmod{p}, \quad \text{где } m = \frac{n}{2} \\
(w_n^2)^k = w_n^{2k} &\not\equiv 1 \pmod{p}, \quad 1 \le k < m.
\end{align}$$

Таким образом, если $w_n$ — корень $n$-й степени из единицы, то $w_n^2$ — корень $\frac{n}{2}$-й степени. Следовательно, для всех меньших степеней двойки тоже существуют нужные корни, и их можно вычислить через $w_n$.

Для обратного ДПФ нам нужен обратный элемент $w_n^{-1}$. Но по простому модулю обратный всегда существует.

Итак, все нужные свойства доступны и в модулярной арифметике, при условии, что у нас есть достаточно большой модуль $p$, для которого существует корень $n$-й степени из единицы.

Например, можно взять модуль $p = 7\,340\,033$, $w_{2^{20}} = 5$. Если этого недостаточно, нужно подобрать другую пару. Можно использовать тот факт, что для модулей вида $p = c \cdot 2^k + 1$ (где $p$ простое) всегда существует корень $2^k$-й степени из единицы. Можно показать, что таким корнем будет $g^c$, где $g$ — [первообразный корень](https://cp-algorithms.com/algebra/primitive-root.html) по модулю $p$.

```cpp
const int mod = 7340033;
const int root = 5;
const int root_1 = 4404020;
const int root_pw = 1 << 20;

void fft(vector<int> & a, bool invert) {
    int n = a.size();

    for (int i = 1, j = 0; i < n; i++) {
        int bit = n >> 1;
        for (; j & bit; bit >>= 1)
            j ^= bit;
        j ^= bit;

        if (i < j)
            swap(a[i], a[j]);
    }

    for (int len = 2; len <= n; len <<= 1) {
        int wlen = invert ? root_1 : root;
        for (int i = len; i < root_pw; i <<= 1)
            wlen = (int)(1LL * wlen * wlen % mod);

        for (int i = 0; i < n; i += len) {
            int w = 1;
            for (int j = 0; j < len / 2; j++) {
                int u = a[i+j], v = (int)(1LL * a[i+j+len/2] * w % mod);
                a[i+j] = u + v < mod ? u + v : u + v - mod;
                a[i+j+len/2] = u - v >= 0 ? u - v : u - v + mod;
                w = (int)(1LL * w * wlen % mod);
            }
        }
    }

    if (invert) {
        int n_1 = inverse(n, mod);
        for (int & x : a)
            x = (int)(1LL * x * n_1 % mod);
    }
}
```

Здесь функция `inverse` вычисляет модулярный обратный (см. [Модулярный мультипликативный обратный](https://cp-algorithms.com/algebra/module-inverse.html)). Константы `mod`, `root`, `root_pw` определяют модуль и корень, а `root_1` — это обратный к `root` по модулю `mod`.

На практике эта реализация работает медленнее версии с комплексными числами (из-за большого количества операций по модулю), но имеет ряд преимуществ: меньшее потребление памяти и отсутствие ошибок округления.

---

## Умножение по произвольному модулю

Хотим достичь той же цели, что и в предыдущем разделе: перемножить два многочлена $A(x)$ и $B(x)$ и вычислить коэффициенты по модулю $M$. Но теоретико-числовое преобразование работает только для определённых простых чисел. Что делать, если модуль не имеет нужной формы?

Один из вариантов — выполнить несколько NTT с различными простыми числами вида $c \cdot 2^k + 1$, а затем применить [Китайскую теорему об остатках](https://cp-algorithms.com/algebra/chinese-remainder-theorem.html) для вычисления конечных коэффициентов.

Другой вариант — разложить многочлены $A(x)$ и $B(x)$ на два меньших каждый:

$$\begin{align}
A(x) &= A_1(x) + A_2(x) \cdot C \\
B(x) &= B_1(x) + B_2(x) \cdot C
\end{align}$$

где $C \approx \sqrt{M}$.

Тогда произведение $A(x)$ и $B(x)$ можно представить как:

$$A(x) \cdot B(x) = A_1(x) \cdot B_1(x) + \left(A_1(x) \cdot B_2(x) + A_2(x) \cdot B_1(x)\right)\cdot C + \left(A_2(x) \cdot B_2(x)\right)\cdot C^2$$

Многочлены $A_1$, $A_2$, $B_1$ и $B_2$ содержат только коэффициенты, меньшие $\sqrt{M}$, поэтому коэффициенты всех произведений меньше $M \cdot n$ — это обычно достаточно мало, чтобы обходиться обычными типами с плавающей точкой.

Этот подход требует вычисления произведений многочленов с меньшими коэффициентами (обычным БПФ и обратным БПФ), а затем исходное произведение восстанавливается за $O(n)$ модулярными сложениями и умножениями.

---

## Применения

ДПФ применяется для решения огромного количества задач, которые на первый взгляд никак не связаны с умножением многочленов.

### Все возможные суммы

Даны два массива $a[]$ и $b[]$. Нужно найти все возможные суммы $a[i] + b[j]$ и для каждой суммы посчитать, сколько раз она встречается.

Например, для $a = [1, 2, 3]$ и $b = [2, 4]$:
- сумму $3$ можно получить $1$ способом
- сумму $4$ — $1$ способом
- сумму $5$ — $2$ способами
- сумму $6$ — $1$ способом
- сумму $7$ — $1$ способом

Построим для массивов $a$ и $b$ два многочлена $A$ и $B$. Числа из массива станут показателями степени многочлена ($a[i] \Rightarrow x^{a[i]}$); коэффициент — это число вхождений значения в массив.

Перемножив эти два многочлена за $O(n \log n)$, получим многочлен $C$, в котором показатели степени соответствуют возможным суммам, а коэффициенты — числу способов получить эту сумму.

Демонстрация на примере:

$$(1 \cdot x^1 + 1 \cdot x^2 + 1 \cdot x^3)(1 \cdot x^2 + 1 \cdot x^4) = 1 \cdot x^3 + 1 \cdot x^4 + 2 \cdot x^5 + 1 \cdot x^6 + 1 \cdot x^7$$

### Все возможные скалярные произведения

Даны два массива $a[]$ и $b[]$ длины $n$. Нужно вычислить скалярные произведения $a$ со всеми циклическими сдвигами $b$.

Создадим два новых массива размера $2n$: реверсируем $a$ и допишем $n$ нулей; а $b$ просто скопируем «сам в себя» (то есть удвоим). Перемножив эти массивы как многочлены и посмотрев на коэффициенты $c[n-1], c[n], \dots, c[2n-2]$ произведения $c$, получим:

$$c[k] = \sum_{i+j=k} a[i] b[j]$$

Поскольку $a[i] = 0$ для $i \ge n$:

$$c[k] = \sum_{i=0}^{n-1} a[i] b[k-i]$$

Легко увидеть, что это сумма — скалярное произведение вектора $a$ с $(k - (n - 1))$-м циклическим левым сдвигом $b$. Эти коэффициенты и есть ответ на задачу, и мы получили его за $O(n \log n)$.

(Заметьте, что $c[2n-1]$ даёт $n$-й циклический сдвиг, который совпадает с $0$-м, так что отдельно его учитывать не нужно.)

### Две полоски

Даны две булевы полоски (циклические массивы из $0$ и $1$) $a$ и $b$. Нужно найти все способы приложить первую полоску ко второй, чтобы ни в одной позиции $1$ из первой полоски не оказалась рядом с $1$ из второй.

Эта задача мало чем отличается от предыдущей. «Приложить» две полоски — значит выполнить циклический сдвиг второго массива; полоски можно приложить, если скалярное произведение этих массивов равно $0$.

### Поиск подстроки

Даны две строки: текст $T$ и шаблон $P$, состоящие из строчных букв. Нужно найти все вхождения шаблона в текст.

Создадим многочлен для каждой строки ($T[i]$ и $P[i]$ — числа от $0$ до $25$, соответствующие буквам алфавита):

$$A(x) = a_0 x^0 + a_1 x^1 + \dots + a_{n-1} x^{n-1}, \quad n = |T|$$

где

$$a_i = \cos(\alpha_i) + i \sin(\alpha_i), \quad \alpha_i = \frac{2 \pi T[i]}{26}.$$

И

$$B(x) = b_0 x^0 + b_1 x^1 + \dots + b_{m-1} x^{m-1}, \quad m = |P|$$

где

$$b_i = \cos(\beta_i) - i \sin(\beta_i), \quad \beta_i = \frac{2 \pi P[m-i-1]}{26}.$$

Заметьте, что выражение $P[m-i-1]$ явно реверсирует шаблон.

Коэффициент с номером $(m-1+i)$ произведения $C(x) = A(x) \cdot B(x)$ говорит нам, встречается ли шаблон в тексте на позиции $i$:

$$c_{m-1+i} = \sum_{j = 0}^{m-1} a_{i+j} \cdot b_{m-1-j} = \sum_{j=0}^{m-1} \left(\cos(\alpha_{i+j}) + i \sin(\alpha_{i+j})\right) \cdot \left(\cos(\beta_j) - i \sin(\beta_j)\right)$$

где $\alpha_{i+j} = \frac{2 \pi T[i+j]}{26}$ и $\beta_j = \frac{2 \pi P[j]}{26}$.

Если есть совпадение, то $T[i+j] = P[j]$ и потому $\alpha_{i+j} = \beta_j$. Тогда (используя основное тригонометрическое тождество):

$$\begin{align}
c_{m-1+i} &= \sum_{j = 0}^{m-1} \left(\cos(\alpha_{i+j}) + i \sin(\alpha_{i+j})\right) \cdot \left(\cos(\alpha_{i+j}) - i \sin(\alpha_{i+j})\right) \\
&= \sum_{j = 0}^{m-1} \cos(\alpha_{i+j})^2 + \sin(\alpha_{i+j})^2 = \sum_{j = 0}^{m-1} 1 = m
\end{align}$$

Если совпадения нет, значит, как минимум один символ отличается, и одно из произведений $a_{i+j} \cdot b_{m-1-j}$ не равно $1$, что приводит к $c_{m-1+i} \ne m$.

### Поиск подстроки с подстановочными символами

Это расширение предыдущей задачи. Теперь шаблон может содержать символ-джокер $*$, который соответствует любому символу. Например, шаблон $a*c$ встречается в тексте $abccaacc$ на трёх позициях: индексы $0$, $4$ и $5$.

Создаём те же самые многочлены, только полагаем $b_i = 0$, если $P[m-i-1] = *$. Если в $P$ всего $x$ джокеров, то совпадение $P$ в $T$ на позиции $i$ определяется условием $c_{m-1+i} = m - x$.

---

## Задачи для практики

- [SPOJ — POLYMUL](http://www.spoj.com/problems/POLYMUL/)
- [SPOJ — MAXMATCH](http://www.spoj.com/problems/MAXMATCH/)
- [SPOJ — ADAMATCH](http://www.spoj.com/problems/ADAMATCH/)
- [Codeforces — Yet Another String Matching Problem](http://codeforces.com/problemset/problem/954/I)
- [Codeforces — Lightsabers (hard)](http://codeforces.com/problemset/problem/958/F3)
- [Codeforces — Running Competition](https://codeforces.com/contest/1398/problem/G)
- [Kattis — A+B Problem](https://open.kattis.com/problems/aplusb)
- [Kattis — K-Inversions](https://open.kattis.com/problems/kinversions)
- [Codeforces — Dasha and cyclic table](http://codeforces.com/contest/754/problem/E)
- [CodeChef — Expected Number of Customers](https://www.codechef.com/COOK112A/problems/MMNN01)
- [CodeChef — Power Sum](https://www.codechef.com/SEPT19A/problems/PSUM)
- [Codeforces — Centroid Probabilities](https://codeforces.com/problemset/problem/1667/E)

---

*Перевод выполнен с английской версии статьи: <https://cp-algorithms.com/algebra/fft.html>. Оригинал распространяется под лицензией CC BY-SA 4.0.*
